export interface MovieData {
    id: number;
    name: string;          // instead of "name"
    price: number;
    currency: string;
    image: string;         // optional: if you want to include a poster image
    // posterUrl: string;      // instead of "image"
    // durationMinutes: number; // optional: typical for a movie
    // genre: string;          // optional: helpful for filtering
    // showtimes?: string[];   // optional: if your app supports multiple times per movie
}
